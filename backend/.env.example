# --- OMNI (Qwen3.5-Omni via yibuapi) -----------------------------------
# Verified: base URL is https://yibuapi.com (api.yibuapi.com does not resolve).
# Models on this key: qwen3.5-omni-flash, qwen3.5-omni-plus,
# qwen3.5-omni-plus-realtime, qwen3.8-omni-flash, gemini-3.1-flash-live-preview.
# Everything OMNI-specific lives in call_omni() in main.py.
OMNI_BASE_URL=https://yibuapi.com
OMNI_API_KEY=sk-BdMbRIcHWyYapARTDMh7V3as6rVBOkjAoY1eh0VleauRpLeg
OMNI_MODEL=qwen3.5-omni-flash

# --- Zip (HTN staging: https://staging-api.zip.com) ---------------------
# Create a key at the Zip company you were given in #spons-zip-2026.
# Order with Zip creates a Request, which is what the Zip website lists.
ZIP_API_URL=https://staging-api.zip.com
ZIP_BASE_URL=https://staging-api.zip.com
ZIP_API_KEY=REPLACE_ME
ZIP_API_VERSION=2024-06-06
ZIP_CURRENCY=USD
ZIP_VENDOR_NAME=Loblaws
ZIP_HARDWARE_VENDOR_NAME=MLH
ZIP_WORKFLOW_NAME=[Do not EDIT] Basic Request a Purchase
ZIP_SUBSIDIARY_NAME=Zip - Modern Spend Approvals
# Optional: your login email on that Zip company, so the Request is yours.
# ZIP_REQUESTER_EMAIL=you@example.com

PORT=3000

# Luxonis OAK camera: max USB speed (HIGH = USB2, the safe default; SUPER = USB3)
# OAK_MAX_USB=HIGH
